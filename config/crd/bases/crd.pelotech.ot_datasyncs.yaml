---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasyncs.crd.pelotech.ot
spec:
  group: crd.pelotech.ot
  names:
    kind: DataSync
    listKind: DataSyncList
    plural: datasyncs
    shortNames:
    - ds
    singular: datasync
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current phase of the DataSync.
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: The ID of the workspace being synced.
      jsonPath: .spec.workspaceId
      name: WorkspaceID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: DataSync is the Schema for the datasyncs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSyncSpec defines the desired state of DataSync.
            properties:
              askForDiskSpace:
                type: boolean
              certConfigMap:
                type: string
              secretRef:
                type: string
              storageClass:
                type: string
              version:
                type: string
              vms:
                description: |-
                  VMS is a list of virtual machines to be synced.
                  Each VM is identified by a name, URL, and sourceType.
                items:
                  description: VMS represents a single virtual machine to be synced.
                  properties:
                    diskSize:
                      description: DiskSize specifies the size of the disk, e.g.,
                        "10Gi", "500Mi".
                      pattern: ^[0-9.]+[A-Za-z]+$
                      type: string
                    name:
                      minLength: 1
                      type: string
                    sourceType:
                      enum:
                      - s3
                      - registry
                      type: string
                    url:
                      minLength: 1
                      type: string
                  required:
                  - diskSize
                  - name
                  - sourceType
                  - url
                  type: object
                minItems: 0
                type: array
              workspaceId:
                description: The unique identifier for the workspace to be synced.
                minLength: 1
                type: string
            required:
            - askForDiskSpace
            - secretRef
            - version
            - vms
            - workspaceId
            type: object
          status:
            description: DataSyncStatus defines the observed state of DataSync.
            properties:
              conditions:
                description: Conditions of the DataSync resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: A human-readable message providing more details about
                  the current phase.
                type: string
              phase:
                enum:
                - Queued
                - Syncing
                - Completed
                - Failed
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

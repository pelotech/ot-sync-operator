apiVersion: v1
kind: Secret
metadata:
  name: "lab-vm-images-cache-s3-creds"
type: Opaque
stringData:
  # To get these values for real run kubectl get secret lab-vm-images-cache-s3-creds -o json | jq -r '.data | map_values(@base64d) ' in ot-dev
  accessKeyId: "FOO"
  secretKey: "BAR"
